

export function checkPath(props:string) {
    switch (props) {
        case '/pages/home':
            const page = 'painel'
            return  page  
              
        default:
            break;
    }
}